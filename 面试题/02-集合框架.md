# 02 - 集合框架

---

## 一、整体架构

1. Java 集合框架的整体结构？Collection 和 Map 的关系？
2. List、Set、Queue 的区别和使用场景？
3. Comparable 和 Comparator 的区别？

## 二、List

1. ArrayList 和 LinkedList 的区别？底层数据结构？
2. ArrayList 的扩容机制？初始容量和扩容因子？
3. ArrayList 和 Vector 的区别？为什么不推荐用 Vector？
4. CopyOnWriteArrayList 的原理和使用场景？

## 三、Map

1. HashMap 的底层实现原理？（数组 + 链表 + 红黑树）
2. HashMap 的 put 流程？hash 计算方式？为什么要高位异或？
3. HashMap 在 JDK 1.7 和 1.8 的区别？为什么 1.7 会出现死循环？
4. HashMap 的扩容机制？为什么容量必须是 2 的幂？
5. HashMap 的线程安全问题？有哪些线程安全的替代方案？
6. ConcurrentHashMap 的实现原理？JDK 1.7（分段锁）vs 1.8（CAS + synchronized）？
7. LinkedHashMap 的实现原理？如何用它实现 LRU 缓存？
8. TreeMap 的底层结构？红黑树的基本性质？
9. WeakHashMap 的作用和使用场景？

## 四、Set

1. HashSet 的底层实现？和 HashMap 的关系？
2. LinkedHashSet 和 TreeSet 的区别？
3. 如何保证 Set 中元素的唯一性？

## 五、Queue

1. PriorityQueue 的底层实现？
2. ArrayDeque vs LinkedList 作为队列的性能对比？
3. BlockingQueue 的实现类有哪些？各自的特点？
4. DelayQueue 的使用场景？

## 六、通用问题

1. fail-fast 和 fail-safe 机制的区别？
2. Iterator 和 ListIterator 的区别？
3. Collections 工具类的常用方法？
4. Arrays.asList() 的坑？返回的 List 能不能 add/remove？
